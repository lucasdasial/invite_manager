<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_all_guest">

    <fragment
        android:id="@+id/nav_all_guest"
        android:name="com.dasial.invitemanager.ui.home.AllGuestFragment"
        android:label="Todos Convidados"
        tools:layout="@layout/fragment_all_guest" />

    <fragment
        android:id="@+id/nav_presents"
        android:name="com.dasial.invitemanager.ui.Present.PresentFragment"
        android:label="Presentes"
        tools:layout="@layout/fragment_present" />

    <fragment
        android:id="@+id/nav_ausents"
        android:name="com.dasial.invitemanager.ui.Ausents.AusentsFragment"
        android:label="Ausentes"
        tools:layout="@layout/fragment_ausents" />
</navigation>